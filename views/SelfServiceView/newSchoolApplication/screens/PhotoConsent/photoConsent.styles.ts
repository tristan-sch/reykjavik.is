import styled from 'styled-components';
import { SULD_25 } from 'utils/hannaColors';

export const StyledAlert = styled.div`
  .Alert {
    background: ${SULD_25};
  }
`;

export const StyledWrapper = styled.div`
  .Radio__label {
    font-weight: bold;
  }
`;
